# 小饭 Agent 工作流

> 本文档面向开发者与 Codex Agent，用于说明“小饭”中 Agent 如何理解用户需求、读取上下文、规划任务、调用工具并完成用户目标。
>
> 产品需求与业务规则以 `PRD.md` 为准；项目安装、配置与启动方式以 `README.md` 为准。
>
> 本文档只描述 Agent 的核心工作流，不重复页面功能、部署方式和底层工程实现。

---

## 1. Agent 整体架构

“小饭”采用“Agent + 确定性工具”的混合架构。

```text
用户
 ↓
主 Agent
 ↓
理解用户意图
 ↓
读取上下文 / 长期记忆
 ↓
制定执行计划
 ↓
选择 Skill / Tool
 ↓
执行工具
 ↓
获得执行结果（Observation）
 ↓
判断任务是否完成
 ↓
生成回答 / 执行动作
```

其中：

### Agent 负责

* 理解自然语言；
* 判断用户意图；
* 根据上下文制定任务计划；
* 决定调用哪个 Skill / Tool；
* 根据工具结果判断是否需要继续执行；
* 组织最终回答；
* 根据用户目标提供个性化建议。

### 确定性工具负责

* BMI 计算；
* 热量预算计算；
* 蛋白质目标计算；
* 营养数据查询；
* 餐厅搜索；
* 用户状态读取；
* 饮食记录写入；
* 体重趋势计算；
* 安全规则判断。

原则：

> 可以通过确定性代码完成的计算，不交给大模型自由生成。

---

# 2. Agent 核心执行循环

所有需要 Agent 处理的用户请求统一进入主 Agent。

```text
用户输入
   ↓
理解意图
   ↓
读取用户上下文
   ↓
制定计划
   ↓
选择 Skill / Tool
   ↓
执行工具
   ↓
获得执行结果
   ↓
任务是否完成？
   │
   ├── 否
   │    ↓
   │  根据结果调整计划
   │    ↓
   │  继续调用工具
   │
   └── 是
        ↓
   是否涉及用户数据修改？
        │
        ├── 否 → 生成最终回答
        │
        └── 是
             ↓
          生成确认提案
             ↓
          用户确认
             ↓
          写入用户数据
```

该循环可以概括为：

```text
理解
 ↓
上下文
 ↓
规划
 ↓
工具调用
 ↓
观察结果
 ↓
必要时重新规划
 ↓
回答 / 行动
```

即：

**Understand → Context → Plan → Tool → Observe → Re-plan → Respond / Act**

Agent 必须设置最大执行轮次，禁止无限循环调用工具。

---

# 3. 餐食推荐工作流

这是“小饭”最核心的 Agent 场景。

典型用户请求：

* “我中午吃什么？”
* “晚饭帮我选一下。”
* “今天在家吃什么？”
* “附近有什么适合我的餐厅？”

Agent 首先读取当前用户上下文：

```text
用户请求
   ↓
读取用户状态
   │
   ├── 减脂目标
   ├── 今日热量预算
   ├── 今日已摄入
   ├── 今日剩余热量
   ├── 饮食偏好
   ├── 忌口
   └── 最近相关上下文
   ↓
判断推荐场景
```

随后进入两条推荐路径：

```text
              餐食推荐
                 ↓
        判断用户当前场景
          ↙             ↘
       在家吃            出去吃
         ↓                 ↓
     DIY Skill       餐厅推荐 Skill
         ↓                 ↓
   生成家庭食谱        搜索附近真实餐厅
         ↓                 ↓
   营养 / 热量计算     菜单 / 条件核验
          ↘              ↙
             主 Agent
                ↓
         综合用户当前状态
                ↓
             推荐结果
```

Agent 不直接凭空生成所有数据。

涉及营养、热量、餐厅位置、菜单等信息时，应优先调用对应工具获取结果。

---

# 4. 拍照记餐工作流

用户可以通过上传食物照片完成饮食记录。

```text
用户上传食物照片
        ↓
视觉模型识别
        ↓
识别食物及主要组成
        ↓
调用营养工具
        ↓
估算营养与热量
        ↓
Agent 判断信息是否充分
        │
        ├── 信息不足
        │      ↓
        │   进行一次关键追问
        │      ↓
        │   用户补充信息
        │
        └── 信息充分
               ↓
          生成餐食记录提案
               ↓
            用户确认
               ↓
          写入饮食记录
               ↓
          更新今日已摄入
               ↓
          更新今日剩余预算
```

Agent 可以针对明显影响热量判断的信息进行一次关键追问，例如：

* 食物份量；
* 烹饪方式；
* 是否添加油、酱料等；
* 图片无法判断的关键配料。

原则：

> Agent 可以估算，但不能把不确定信息描述成精确事实。

所有热量估算统一使用单一近似值；复杂菜品可以说明主要误差来源。

---

# 5. 主动干预工作流

“小饭”不仅响应用户请求，也可以根据用户目标和当前状态进行主动提醒。

```text
系统定时触发
      ↓
读取用户当前状态
      ↓
检查：
饮食记录
体重趋势
目标进度
当前时间
      ↓
判断是否满足提醒条件
      │
      ├── 否 → 结束
      │
      └── 是
           ↓
        生成提醒任务
           ↓
        Agent 组织提醒内容
           ↓
        推送给用户
           ↓
        用户产生响应
           ↓
        进入正常 Agent 对话
```

例如：

```text
接近午餐时间
+
用户尚未规划午餐
        ↓
触发午餐提醒
        ↓
“中午还没想好吃什么？
我可以按照你今天剩余的热量帮你选。”
```

体重相关干预优先依据趋势，而不是单日体重变化。

---
# 6. Agent 安全与执行边界

Agent 的自主决策必须运行在产品定义的安全规则与执行边界内。
具体安全规则以 `PRD.md` 为准。

## 6.1 安全规则处理流程

当用户请求、用户目标或 Agent 执行计划触碰产品安全规则时，
Agent 不终止原计划，而是在明确提示相关风险后继续执行。

用户请求 / 用户目标
        ↓
安全规则检查
        ↓
是否触碰安全规则？
        │
        ├── 否
        │    ↓
        │  正常执行原计划
        │
        └── 是
             ↓
        获取触发的风险原因
             ↓
        向用户明确提示风险
             ↓
        继续执行原计划

### 处理原则

- 触碰安全规则不直接终止任务。
- Agent 必须明确说明触发了什么风险以及风险原因。
- 完成风险提示后，继续执行用户原计划。
- Agent 不得隐藏、弱化或省略已经触发的风险提示。
- 是否触碰安全规则由确定性规则判断，Agent 负责解释风险并继续后续任务。
### 示例

用户首次建档时设置：

- 当前体重：80 kg
- 目标体重：60 kg
- 目标周期：2 周

系统计算后检测到目标减重速度触发安全规则。

系统应：

1. 明确展示触发的风险及原因；
2. 不因触发安全规则终止任务；
3. 完成风险提示后，继续按照用户设定的原计划生成后续结果。

# 6. Agent 安全与执行边界

Agent 的自由决策必须运行在确定性规则之上。

## 6.1 安全规则处理流程

安全规则采用按需检查机制。

当以下信息首次产生或发生变化时，执行对应的安全规则检查：

- 用户首次设定减脂目标；
- 用户修改目标体重或目标日期；
- 系统调整可能影响安全状态的计划；
- 用户在 Agent 对话中提出可能触碰安全规则的新要求。

不需要在每一轮普通 Agent 对话中重复执行全部安全检查。

```text
产生 / 修改安全相关信息
        ↓
执行对应的确定性计算
        ↓
安全规则检查
        ↓
是否触碰安全规则？
        │
        ├── 否
        │    ↓
        │  正常继续
        │
        └── 是
             ↓
        获取风险类型与原因
             ↓
        向用户明确展示风险提示
             ↓
        继续执行原计划
   

## 6.2 确定性计算优先

以下内容由程序或工具计算：

```text
BMI
热量预算
蛋白质目标
今日已摄入
今日剩余预算
体重趋势
安全阈值
```

LLM 可以解释结果，但不得自行修改计算结果。

---

## 6.3 用户确认后才能写入数据

涉及用户状态修改时：

```text
Agent 生成修改内容
        ↓
生成确认提案
        ↓
用户确认
        ↓
执行写入
```

例如：

```text
Agent：
“我识别到这顿午餐约 520 kcal，
要帮你记录到今天的午餐吗？”

用户：
“记录。”

        ↓

写入 meal log
```

Agent 不得在用户未确认的情况下静默修改饮食记录。

---

## 6.4 外部数据不可伪造

涉及：

* 餐厅；
* POI；
* 菜单；
* 营养数据库；

等外部信息时，应优先使用真实工具结果。

如果外部服务不可用或数据无法核验：

```text
无法获得可靠数据
        ↓
降低推荐精度
        ↓
明确告诉用户信息限制
```

不得为了完成回答而生成虚假的实时数据。

---

## 6.5 医疗安全边界

“小饭”定位为减脂与饮食管理 Agent，不是医疗诊断系统。

对于：

* 疾病诊断；
* 药物调整；
* 治疗方案；
* 明显异常健康状态；

Agent 不提供医疗治疗方案，应建议用户咨询专业医疗人员。

---

# 7. Agent 与传统程序的职责边界

“小饭”不是所有功能都使用 Agent。

系统采用：

```text
                 小饭
                  │
        ┌─────────┴─────────┐
        ↓                   ↓
     确定性系统             Agent
        ↓                   ↓
     BMI计算             意图理解
     热量预算             上下文理解
     营养计算             任务规划
     安全规则             Tool选择
     数据写入             多步骤执行
     趋势计算             个性化推荐
```

确定性系统负责：

**准确、稳定、可测试的任务。**

Agent 负责：

**开放式理解、动态决策、多步骤任务和个性化交互。**

Agent 通过 Tool 调用确定性能力，而不是替代这些能力。

---

# 8. 核心设计原则

整个“小饭”Agent 应遵循以下原则：

1. **Agent 负责决策，Tool 负责确定性执行。**
2. **能用确定性代码完成的计算，不交给 LLM 猜测。**
3. **Agent 根据用户上下文动态选择 Skill / Tool，而不是固定执行单一路径。**
4. **Tool 执行结果必须返回 Agent，由 Agent 判断下一步。**
5. **涉及用户数据修改时必须经过用户确认。**
6. **外部数据不可用时明确降级，不生成伪造数据。**
7. **体重管理以趋势为依据，不根据单日波动进行机械调整。**
8. **Agent 必须具有最大执行轮次和失败兜底机制。**


