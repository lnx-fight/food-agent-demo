{
  "name": "fitpilot-agent",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs"
  },
  "engines": { "node": ">=22" }
}

