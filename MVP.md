# 小饭 MVP 设计

## 1. 产品定位

小饭 MVP 是一个**预算驱动的饮食 Agent**：用户输入提示词（如"帮我推荐午餐"），Agent 读取记忆——减肥计划、饮食偏好与忌口、今日已摄入的热量——结合提示词中的当次约束（金钱预算、口味、地点），推荐预算内能吃的 DIY 菜谱或附近餐馆菜品。用户对推荐有确认权，确认后入账并更新今日预算。

核心用户是有明确减重目标、希望通过饮食减重，但每天难以在热量限制内决定"下一顿吃什么"的人：他们需要 Agent 根据口味和忌口，算好并可直接确认的下一餐答案；产品价值是替用户完成这道决策。

核心假设：用户愿意用一句提示词让 Agent 帮忙做"吃什么"的决策，并信任"预算内 + 忌口安全 + 可确认"的 AI 推荐。

## 2. 核心链路（输入 → AI 处理 → 用户确认 → 结果）

MVP 只保留一条主链路：**用户输入提示词 → Agent 读取记忆 → AI 处理 → 用户确认 → 结果**。

```
输入：用户输入提示词（如"中午吃什么""帮我推荐晚餐"）
  可能附带当次约束：金钱预算、口味、地点、时间
        │
        ▼
AI 处理第一步：Agent 读取记忆
 ├─ 减肥计划（目标体重、目标日期、活动水平）
 ├─ 饮食偏好与忌口（档案 + 历史对话）
 ├─ 今日已摄入记录（拍照识别或手动记录得来的热量/蛋白质）
 └─ 历史对话与确认过的记录
        │
        ▼
AI 处理第二步：预算计算与方案生成
 ├─ 计算今日剩余热量/蛋白质预算（计划 − 已摄入）
 ├─ 结合提示词约束与偏好/忌口
 └─ 生成 1–2 个预算内方案（DIY 菜谱 / 附近外食）
        │
        ▼
用户确认：选择方案 / 调整份量 / 换一道
        │
        ▼
结果：方案入账为今日饮食记录 → 更新剩余预算
        │
        └──────────────────────► 沉淀为记忆，成为下一轮"读取"的状态（闭环）
```

拍照识别在链路中的位置：它是"今日已摄入"这一记忆的**输入方式之一**（用户也可以手动记录），不是链路起点。用户先拍照让 Agent 记下热量，之后"帮我推荐"时，Agent 读取的剩余预算就是由这些记录算出来的。

设计要点：整条链路上，**模型输出永远不是最终结果**——推荐方案要用户选择，确认后才入账并更新预算；下一次交互自动带上最新的记忆状态。

## 3. 必须做（MVP 范围）

| 模块 | 内容 | 为什么必须 |
| --- | --- | --- |
| 极简建档 | 身高、体重、目标、活动水平 → 每日热量与蛋白质预算；偏好与忌口 | 链路中"预算"与"忌口"的来源 |
| 提示词输入 | 用户用自然语言发起请求（"帮我推荐午餐"），可附带金钱预算、口味、地点 | 链路起点 |
| 记忆读取 | Agent 读取减肥计划、偏好忌口、今日已摄入记录、历史对话 | "推荐有依据"的前提 |
| 预算计算 | 计划 − 今日已摄入 → 剩余热量/蛋白质预算，分配给本餐并预留后续餐次 | 推荐的核心引擎 |
| 拍照识别（支撑） | 上传图片 → 识别食材 → 营养匹配（本地库 → USDA → 联网降级）→ 热量区间 + 误差来源说明 → 确认入账 | "今日已摄入"记忆的主要输入方式 |
| 用户确认/选择 | 从推荐方案中选择 / 调整份量；识别结果可修正重量、餐次、日期、总热量 | 防幻觉、建立信任、沉淀修正数据 |
| 今日摄入状态 | 已摄入/剩余热量、蛋白质自动更新 | 推荐的前提，闭环成立的根基 |
| 双路径推荐 | "帮我选" → 在家 DIY / 附近外食 | 用户真实的两种决策场景 |
| DIY 推荐 | 预算内 + 可用食材 + 忌口硬约束 + 服务端计算营养 | 闭环完全可控，最稳的推荐路径 |
| 外食推荐 | 预算内 + 偏好排序 + 营业时间过滤（MVP 按店型给建议点法） | 高频场景；暂不做真实菜单核验 |
| 安全约束 | 忌口/过敏硬拦截、受控动作白名单、数值与日期校验 | 健康产品底线 |
| 数据存储 | 档案、体重、饮食记录、最近对话（单机 localStorage 即可） | "读取记忆"环节的数据基础 |

## 4. 暂时不做（及理由）

| 功能 | 不做的原因 |
| --- | --- |
| 体重趋势可视化、目标重算的复杂展示 | 与核心闭环无关，简单记录体重即可 |
| 品牌营养匹配 / 条码精确化 | 营养库 + 降级链够用，不验证核心假设 |
| 外食真实菜单核验 | 依赖三方数据，MVP 用店型建议点法替代 |
| 提醒、打卡、周报、多设备同步 | 留存与商业化后置，MVP 只验证链路是否成立 |
| 开放领域闲聊 | 只保留"记一餐"和"帮我选"两种意图 |
| 修正数据回写（越用越准的学习闭环） | P1：先验证链路跑通，再沉淀数据 |
| 联网搜索精度优化 | 作为兜底可用即可，不作为 MVP 重点 |

## 5. 关键设计决策

1. **单主 Agent + 多工具**：主 Agent 统一持有状态、做意图路由；视觉模型只做感知，营养库 / USDA / Open Food Facts / 高德 / 联网搜索都是工具。MVP 不拆多 Agent。
2. **预算双来源**：热量与蛋白质预算是**系统状态**，由 Agent 从记忆（计划 + 今日记录）读取；金钱预算是**提示词约束**，用户当场说。一个硬过滤、一个软约束。
3. **受控动作**：LLM 只能输出白名单内的动作（保存餐食、修正估算、记录体重、打开推荐），由系统校验后执行，模型不能仅凭文字声称已写入。
4. **诚实的不确定性**：查不到营养数据就明说；热量给区间和误差来源，不假装精确。

## 6. 成功指标

- 提示词 → 确认入账 ≤ 3 步；
- 推荐采纳率（用户选择"就吃这个"）；
- 被采纳餐食热量落在本餐预算内的比例；
- 拍照识别估算被直接确认（无需修正）的比例；
- 单日是否完成"记录 → 推荐 → 入账"完整循环；7 日回访天数。

## 7. Pivot 预案

- 识别修正率过高 → 输入重心从"拍照"切到"文字描述优先、拍照辅助"；
- 推荐采纳率低 → 先排查剩余预算是否真实（记录闭环的问题），再谈匹配质量；
- 外食数据不可靠 → 外食降级为店型建议，集中打磨 DIY。

## 8. 与当前 Demo 的关系

当前 Demo 已覆盖 MVP 的大部分模块：建档（计划/偏好/忌口）、提示词输入、Agent 读取记忆（档案 + 今日已摄入 + 最近对话）、预算计算（mealTargets）、拍照识别与确认入账、DIY 推荐、外食推荐、安全约束。

主要差距：

- 推荐方案缺少"一键入账"的受控动作闭环——用户选中推荐后还不能直接把这道菜记为已摄入；
- 外食仍可简化到店型建议，降低对三方菜单数据的依赖；
- 修正数据尚未回写为学习能力（P1）。

MVP 验证顺序建议：先把"提示词 → 读取记忆 → 推荐 → 一键入账"的主链路跑通，再打磨拍照识别的准确率。
