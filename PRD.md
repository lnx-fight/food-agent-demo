# 小饭 AI 减脂 Agent 产品需求文档（PRD）

## 1. 产品概述

### 1.1 产品名称

**小饭**

### 1.2 产品定位

小饭是一款面向减脂用户的**个人饮食决策 Agent**。

它持续理解用户的减脂目标、体重趋势、当天饮食状态、饮食偏好与现实就餐条件，通过记忆、工具调用和动态决策，帮助用户解决：

> **“按照我今天的真实情况，下一顿到底应该吃什么？”**

小饭不是一次性生成减肥方案的聊天机器人，也不是单纯的热量记录工具。

它围绕用户的长期减脂目标持续工作：

```text
设定减脂目标
      ↓
生成初始计划
      ↓
记录体重与饮食
      ↓
获得真实反馈
      ↓
分析体重趋势与执行情况
      ↓
必要时调整计划
      ↓
根据当天状态推荐下一餐
      ↓
用户执行
      ↓
产生新的体重 / 饮食数据
      ↓
继续观察与调整
```

### 1.3 Demo 形态

响应式 Web 应用。

Demo 重点展示 Agent 的：

* 状态理解；
* 长期记忆；
* Tool Use；
* 多模态理解；
* 动态规划；
* Human-in-the-loop；
* 主动干预；
* 可解释决策。

---

# 2. 用户与核心问题

## 2.1 目标用户

小饭面向：

> 有明确减重目标，希望主要通过饮食管理减脂，但经常不知道在当天热量限制和现实条件下“下一顿应该吃什么”的成年人。

MVP 面向正常健康成年人。

未成年人以及需要医疗营养治疗的特殊人群不属于当前产品范围。

---

## 2.2 核心痛点一：每顿饭都要重新做决策

减脂用户真正困难的往往不是“不知道什么食物健康”。

他们通常知道：

* 少吃高热量食物；
* 多摄入蛋白质；
* 控制总热量；
* 少油少糖。

真正的问题是：

> **这些原则很难直接变成“我这一顿具体吃什么”。**

因为每顿饭的约束都可能发生变化：

```text
今天还剩多少热量？
+
蛋白质还差多少？
+
现在是早餐 / 午餐 / 晚餐？
+
今天已经吃了什么？
+
家里有什么食材？
+
现在有没有时间做饭？
+
用户在哪里？
+
附近有什么餐厅？
+
用户喜欢吃什么？
+
有什么忌口？
```

因此，用户每天甚至每顿都需要重新计算和判断。

小饭希望把这种重复发生的**饮食决策成本**交给 Agent。

最终不是给用户大量知识和选项，而是给出：

> **符合当前约束、可以直接执行的下一餐答案。**

---

## 2.3 核心痛点二：饮食记录成本高

很多用户并非单纯“不愿意记录”，而是：

> **不知道自己吃的这一顿到底应该怎么记录。**

例如用户知道自己吃了宫保鸡丁和米饭，但不知道：

* 食物具体重量；
* 烹饪用油；
* 酱汁；
* 食材比例；
* 对应热量。

传统记录工具往往要求用户先知道这些信息才能完成记录。

小饭通过：

```text
照片 / 自然语言
      ↓
食物识别
      ↓
营养数据匹配
      ↓
估算
      ↓
必要时关键追问
      ↓
用户确认
      ↓
完成记录
```

降低饮食记录门槛。

---

## 2.4 核心 Job To Be Done

当用户正在减脂，并且面对下一顿饭不知道如何选择时：

> **帮用户结合今天真实的饮食状态和现实条件，完成这一顿饭的决策，而不是再给用户一堆需要自己判断的信息。**

---

# 3. 为什么使用 Agent

## 3.1 为什么不是普通推荐系统

“下一顿吃什么”并不是一个固定输入对应固定输出的问题。

用户每次请求的约束不同，例如：

```text
剩余热量
饮食记录
体重趋势
口味
忌口
位置
食材
时间
就餐方式
```

因此系统需要根据当前 Context 动态决定：

1. 需要读取哪些用户状态；
2. 是否需要补充信息；
3. 应调用哪些工具；
4. 应选择 DIY 还是附近餐厅；
5. 工具结果不足时是否需要重新规划；
6. 最终应该推荐什么。

这类任务适合由 Agent 进行动态决策和工具编排。

---

## 3.2 Agent 与确定性程序的边界

小饭不把所有任务交给大模型。

Skill = 场景级、可复用的行为流程（目标、步骤、约束、输出、完成标准）；主 Agent 按意图选择 Skill，Skill 内部调用 Tool 获取确定性结果。当前第一个 Skill 是“下一餐推荐（meal-
  recommendation）”。

整体采用：

```text
                    小饭
                     │
           ┌─────────┴─────────┐
           ↓                   ↓
        Agent              确定性程序 / Tool
           ↓                   ↓
       意图理解              BMI计算
       上下文理解            热量预算
       任务规划              营养计算
       Tool选择             体重趋势
       多步骤执行            数据读取 / 写入
       个性化推荐            外部数据查询
                           安全规则
```

原则：

> **Agent 负责不确定的决策与编排，确定性程序负责确定的计算、查询和执行。**

例如：

用户问：

> “我中午吃什么？”

Agent 判断当前任务需要：

```text
读取今日饮食
→ 获取午餐预算
→ 检查偏好 / 忌口
→ 判断 DIY / 到店
→ 调用对应工具
→ 根据结果推荐
```

但具体的：

```text
剩余热量 = ?
BMI = ?
附近有哪些真实餐厅？
某种食物的营养数据是什么？
```

由程序、数据库或外部 API 完成，而不是让 LLM 猜测。

Agent 的详细执行机制见 `WORKFLOW.md`。

---

# 4. 产品目标与非目标

## 4.1 Demo 目标

通过一个可以完整演示的用户流程证明：

### ① Agent 能理解长期目标

Agent 知道用户：

* 当前体重；
* 目标体重；
* 目标日期；
* 当前计划。

而不是每轮重新询问。

### ② Agent 有状态

Agent 可以读取：

* 用户档案；
* 今日饮食；
* 剩余预算；
* 饮食偏好；
* 忌口；
* 历史体重；
* 相关长期记忆。

### ③ Agent 能调用工具

Agent 可以根据任务动态使用：

* 热量预算；
* 营养查询；
* 食谱；
* 餐厅搜索；
* 用户状态；
* 记忆等能力。

### ④ Agent 能处理多模态输入

用户可以上传食物照片，由系统识别并辅助完成饮食记录。

### ⑤ Agent 能根据反馈调整行为

系统根据用户持续产生的体重和饮食数据判断：

```text
继续当前计划
/
需要调整
/
数据不足，继续观察
```

### ⑥ Agent 的重要行动可解释、可控制

涉及用户数据修改时，由用户确认后执行。

---

## 4.2 用户目标

用户使用小饭后应该能够：

* 快速知道下一顿吃什么；
* 减少减脂过程中的饮食决策成本；
* 低成本记录饮食；
* 理解体重变化是短期波动还是长期趋势；
* 获得与当天实际状态匹配的饮食建议；
* 不需要每次重新解释自己的目标、偏好和忌口。

---

## 4.3 非目标

Demo 阶段不提供：

* 疾病诊断或治疗；
* 减肥药、处方药或手术建议；
* 医疗营养治疗；
* 未成年人自动减脂计划；
* 专业运动课程；
* 外卖交易、支付和配送；
* 医疗级精确热量或体脂测量；
* 真人营养师服务。

---

# 5. 核心用户体验

## 5.1 用户建档

首次使用时收集：

| 信息      | 说明                |
| ------- | ----------------- |
| 年龄      | Demo 仅支持成年人       |
| 生理性别    | 用于基础代谢估算          |
| 身高      | cm                |
| 当前体重    | kg                |
| 目标体重    | kg                |
| 目标日期    | 用于计算目标速度          |
| 活动水平    | 久坐 / 轻度 / 中度 / 高度 |
| 饮食偏好    | 如中餐、素食、不吃辣        |
| 过敏 / 忌口 | 用于推荐硬过滤           |
| 常用就餐方式  | 在家 / 到店 / 混合      |
| 烹饪条件    | 时间、厨具等            |
| 现有食材    | DIY 推荐使用          |

系统生成：

* 当前 BMI；
* 目标 BMI；
* 目标周期；
* 理论目标速度；
* 每日热量预算；
* 蛋白质目标；
* 必要的安全提示。

---

## 5.2 今日计划

今日页面展示用户当前最需要知道的信息：

* 今日热量预算；
* 已记录饮食；
* 剩余热量；
* 蛋白质完成情况；
* 体重趋势；
* 1—3 个今日行动；
* Agent 当前判断。

产品不使用“今天必须减多少克”作为目标。

体重判断优先使用趋势，而不是单日数字。

---

## 5.3 记录饮食

支持：

### 文字记录

例如：

> “午饭吃了一碗米饭、鸡胸肉和炒青菜。”

### 拍照记录

```text
上传照片
→ 食物识别
→ 营养匹配 / 估算
→ 必要时关键追问
→ 用户确认
→ 写入今日饮食
→ 更新剩余预算
```

热量结果统一显示为**单一近似值**。

复杂菜品需要说明主要不确定因素，不做无依据的精确承诺。

---

## 5.4 下一餐推荐

用户可以直接问：

> “我中午吃什么？”

系统读取：

* 今日剩余热量；
* 蛋白质及营养缺口；
* 已记录饮食；
* 偏好；
* 忌口；
* 当前时间；
* 当前餐次；
* 必要的现实条件。

提供两条路径：

### 路径 A：在家 DIY

根据：

```text
剩余预算
+
现有食材
+
烹饪时间
+
厨具
+
偏好 / 忌口
```

生成：

* 菜名；
* 食材克数；
* 制作步骤；
* 预计耗时；
* 单一近似热量；
* 必要的替换方案。

### 路径 B：附近餐厅

根据：

```text
剩余预算
+
用户位置 / 商圈
+
偏好 / 忌口
+
附近真实餐厅数据
```

推荐：

* 餐厅；
* 适合的菜品或店型；
* 点餐调整建议；
* 推荐理由；
* 数据来源与可信度。

Demo 不提供外卖模式。

---

## 5.5 体重趋势与计划调整

用户通过体重页面手动记录体重。

系统持续计算体重趋势。

基本原则：

> **趋势优于单点。**

单日体重上涨或下降不直接触发计划调整。

当有效数据足够时，系统结合：

```text
体重趋势
+
饮食记录完整度
+
当前减脂目标
```

判断是否需要调整后续计划。

---

# 6. MVP 范围

## 6.1 P0：Demo 必须实现

| 模块       | 核心能力            |
| -------- | --------------- |
| 用户建档     | 建立目标与长期用户状态     |
| 初始计划     | 生成热量预算与蛋白质目标    |
| 今日计划     | 展示今日摄入、剩余预算与行动  |
| 体重记录     | 手动记录并查看趋势       |
| 动态调整     | 根据趋势判断是否调整计划    |
| 文字记餐     | 自然语言辅助记录饮食      |
| 拍照记餐     | 图片识别、营养估算、确认入账  |
| Agent 对话 | 处理减脂与饮食相关请求     |
| DIY 推荐   | 根据预算和食材生成下一餐    |
| 附近餐厅     | 搜索附近真实餐厅并推荐     |
| 长期记忆     | 保存影响未来决策的重要信息   |
| 安全规则     | 对目标 BMI 过低、目标减重速度过快进行风险识别与提示 |
| 主动提醒     | 在适当时机提醒用户规划餐食   |

---

## 6.2 P1：后续扩展

Demo 阶段不要求实现：

* Apple Health / Keep 等运动数据接入；
* 智能体脂秤；
* 智能手表；
* 正式餐饮交易平台；
* 真人营养师；
* 家庭多人计划；
* 语音输入与播报。

---

# 7. Agent 核心能力

## 7.1 Context：理解当前状态

Agent 根据当前任务按需读取：

```text
当前用户输入
+
用户档案
+
减脂目标
+
今日饮食
+
剩余预算
+
饮食偏好 / 忌口
+
体重趋势
+
相关历史对话
+
长期记忆
```

Agent 不应要求用户反复提供已经存在的关键信息。

---

## 7.2 Memory：长期记忆

长期记忆用于保存未来仍然有价值的信息，例如：

* 长期饮食偏好；
* 明确忌口；
* 经常使用的就餐方式；
* 用户确认过的重要习惯；
* 对未来推荐有帮助的历史反馈。

并非所有聊天内容都需要进入长期记忆。

---

## 7.3 Planning：动态规划

Agent 不使用固定脚本完成所有任务。

例如：

```text
用户：“中午吃什么？”
        ↓
读取今日状态
        ↓
判断缺少什么信息
        ↓
选择适合的能力
        ↓
调用 Tool
        ↓
观察结果
        ↓
必要时调整计划
        ↓
完成推荐
```

详细 Agent Loop 见 `WORKFLOW.md`。

---

## 7.4 Tool Use：工具调用

Agent 所需的核心工具能力包括：

| 能力     | 作用             |
| ------ | -------------- |
| 用户状态读取 | 获取档案、今日摄入和当前计划 |
| 餐次预算   | 获得当前餐次可用预算     |
| 营养查询   | 获取食物营养数据       |
| DIY 食谱 | 根据现实约束生成食谱     |
| 餐厅搜索   | 获取附近真实餐厅       |
| 菜单核验   | 获取能够验证的菜单信息    |
| 体重趋势   | 获得趋势计算结果       |
| 长期记忆   | 读取影响当前任务的信息    |
| 餐食记录   | 用户确认后修改饮食状态    |

具体 Tool 的函数设计属于实现层，不在 PRD 中定义。

---

## 7.5 Multimodal：多模态

用户可以通过食物照片提供信息。

视觉模型负责识别图片内容，Agent 负责结合：

* 图片结果；
* 用户状态；
* 营养数据；
* 不确定因素；

决定：

```text
直接生成记录提案
/
先进行关键追问
```

---

## 7.6 Human-in-the-loop

涉及用户数据修改时：

```text
Agent生成提案
      ↓
用户确认
      ↓
执行写入
```

例如：

> “这顿午餐估算约 520 kcal，要记录到今天午餐吗？”

只有用户确认后才能写入饮食记录。

---

# 8. 核心产品规则

## 8.1 体重趋势

使用近期趋势判断减脂进展。

原则：

* 单日变化不直接调整计划；
* 有效数据不足时继续观察；
* 趋势接近目标时维持计划；
* 长期明显偏离目标时再考虑调整。

---

## 8.2 动态调整

Demo 使用**规则引擎进行核心判断，LLM 负责解释结果。**

| 状态          | 行动            |
| ----------- | ------------- |
| 有效数据不足      | 不调整，继续记录      |
| 趋势符合目标      | 保持当前计划        |
| 单日明显波动      | 不立即调整         |
| 连续趋势偏慢且记录完整 | 小幅调整或生成干预任务   |
| 趋势偏慢但记录不完整  | 优先改善记录        |

每次调整应能够说明：

> **发生了什么 → 为什么判断 → 做了什么调整。**

### 8.2.1 有效数据不足判定

观察窗口为滚动近 14 天（含今天，按 Asia/Shanghai 日期计算）。

有效体重记录需同时满足：

* 数值在合理范围；
* 日期非未来且落在观察窗口内；
* 同一天多条记录只取当天最新一条，计 1 个有效记录日。

判定规则：

| 状态       | 判定条件 |
| ---------- | -------- |
| 趋势可计算 | 窗口内有效记录日 ≥ 7 天 |
| 可触发调整 | 窗口内有效记录日 ≥ 7 天，且最近一次记录在 3 天内 |
| 有效数据不足 | 有效记录日 < 7 天，或最近一次记录距今 > 3 天 |
| 数据过期   | 最近一次记录距今 > 3 天，即使记录数足够，也先不调整 |

以上判定由确定性代码 / 规则引擎计算，LLM 只解释结果，不得修改阈值。

### 8.2.2 记录是否完整判定

先区分“覆盖”和“完整”：

| 概念         | 定义 |
| ------------ | ---- |
| 有效饮食记录 | 已确认入账的餐食记录，含日期、餐次、热量；待确认估算不计入 |
| 当日覆盖     | 当天至少 1 条有效饮食记录 |
| 当日疑似漏记 | 到了某餐时段但无该餐有效记录 |
| 当日完整     | 当日有覆盖，且没有疑似漏记信号 |

观察窗口同为滚动近 14 天，记录完整度采用“有效记录天数覆盖率”：

* 覆盖天数 = 窗口内当日覆盖的自然日数；
* 完整度 = 覆盖天数 ÷ 14；
* 记录完整：完整度 ≥ 70%（即 ≥ 10 天有覆盖），且最近 3 天有覆盖；
* 记录不完整：完整度 < 70%，或窗口内连续 ≥ 2 天无任何有效饮食记录；
* 疑似漏记只用于生成追问或“优先改善记录”任务，不直接判整个窗口不完整，除非连续、频繁出现。

今天只记一顿饭：计入当日覆盖；若同时存在明显漏餐信号，则当日标记“疑似漏记”，但不因此把整日视为空白。

> 示例：
>
> * 14 天内只有 3 条有效体重记录，但体重下降 → 有效数据不足，不调整。
> * 体重记录 8 天，但饮食仅覆盖 3 天 → 优先改善记录，不调整。
> * 体重趋势偏慢，饮食连续 14 天覆盖完整且无疑似漏记 → 才小幅调整并说明原因。

---

## 8.3 推荐原则

下一餐推荐优先考虑：

1. 安全与忌口；
2. 当餐营养匹配；
3. 用户偏好；
4. 当前现实条件；
5. 时间 / 距离；
6. 数据可信度。

Demo 不需要复杂机器学习排序，可使用可解释规则完成。

---

## 8.4 估算原则

对于无法获得精确数据的食物：

* 返回单一近似值；
* 明确说明为估算；
* 给出主要误差来源；
* 必要时追问最影响结果的信息；
* 用户可以修正。

---

# 9. 安全与降级

## 9. 减脂安全规则

安全规则由确定性程序判断，不由 LLM 自行判断或修改阈值。

当前 Demo 包含以下两类减脂安全规则：

| 风险类型 | 触发条件 | 系统行为 |
| --- | --- | --- |
| 目标 BMI 风险 | 目标 BMI < 18.5 | 明确风险提示 → 继续原计划 |
| 减重速度风险 | 理论目标减重速度 > 0.9 kg/周 | 明确风险提示 → 继续原计划 |

### 判断规则

目标 BMI：

```text
目标 BMI =
目标体重（kg） / 身高（m）²
```
---

## 9.2 LLM 执行边界

### LLM 执行边界

LLM 不得：

- 自行修改确定性计算结果；
- 自行修改安全规则或安全阈值；
- 忽略、隐藏或弱化已触发的安全风险；
- 编造不存在的餐厅；
- 编造无法核验的实时菜单；
- 将估算描述为精确事实；
- 未经用户确认修改关键用户状态。

涉及明显健康异常、疾病、药物或治疗方案的问题，不属于上述减脂安全规则的判断范围。
小饭用于一般健康与饮食管理，不构成医疗诊断或治疗建议。
---

## 9.3 外部服务降级

外部数据不可用时必须明确降级。

| 能力   | 正常方式       | 降级           |
| ---- | ---------- | ------------ |
| 食物图片 | 多模态识别      | 用户文字描述       |
| 营养数据 | 本地 / 权威数据库 | 模型辅助估算并降低可信度 |
| 餐厅   | 高德 POI     | 用户提供商圈 / 店型  |
| 菜单   | 可核验公开数据    | 推荐店型和点餐策略    |

原则：

> **宁可降低推荐精度，也不伪造真实世界数据。**

---

# 10. 数据与第三方能力

Demo 主要依赖：

### 用户数据

* 用户档案；
* 减脂目标；
* 饮食记录；
* 体重记录；
* 用户偏好；
* 长期记忆。

### 营养数据

优先使用结构化营养数据。

无法获得精确数据时允许估算，但必须明确不确定性。

### 视觉能力

用于食物照片理解。

### 本地餐饮数据

通过地图 / POI 服务获得真实附近餐厅。

---

# 11. 非功能需求

| 维度   | 要求                    |
| ---- | --------------------- |
| 可解释性 | 重要调整能够说明依据            |
| 可靠性  | Tool 失败时降级，不编造        |
| 可测试性 | 关键计算和规则可测试            |
| 可观测性 | 能查看 Agent 的关键 Tool 调用 |
| 用户控制 | 重要状态修改需要确认            |
| 移动端  | 核心流程支持移动端             |
| 成本   | 控制模型调用次数与上下文长度        |

---

```markdown
# 12. 验收标准

## 12.1 核心产品能力

### 用户准入与建档

* [ ] 年龄 < 18 岁的用户无法创建档案和不会进入减脂计划生成流程；
* [ ] 用户档案可以保存减脂目标、饮食偏好、忌口及必要的基础信息。

### 确定性计算

* [ ] BMI 由确定性程序计算；
* [ ] 目标 BMI 由确定性程序计算；
* [ ] 理论目标减重速度由确定性程序计算；
* [ ] 每日热量预算和相关数值不由 LLM 自由生成或修改；
* [ ] 关键计算规则具备可测试性。

### 安全规则

* [ ] 当目标 BMI < 18.5 时，系统能够触发“目标 BMI 过低”风险；
* [ ] 当理论目标减重速度 > 0.9 kg/周时，系统能够触发“目标减重速度过快”风险；
* [ ] 触发安全规则后能够返回明确的风险类型；
* [ ] 触发安全规则后能够向用户展示风险原因；
* [ ] Agent 不会忽略、隐藏或弱化已触发的风险；
* [ ] 完成风险提示后，系统可以继续执行用户原计划；
* [ ] 当目标体重或目标日期发生变化时，系统会重新执行对应的安全规则检查；
* [ ] LLM 无法自行修改安全阈值或覆盖确定性安全判断。

### 体重与动态调整

* [ ] 用户可以手动记录体重；
* [ ] 用户可以查看近期体重趋势；
* [ ] 单日体重变化不会直接触发计划调整；
* [ ] 系统可以根据有效历史数据判断趋势状态；
* [ ] 数据不足时不会机械调整计划；
* [ ] 动态调整结果能够说明判断依据与调整原因；
* [ ] 有效记录日 < 7 天时，系统不调整，只提示继续记录；
* [ ] 有效记录日足够但最近 3 天无记录时，系统不调整；
* [ ] 饮食记录覆盖率 < 70% 时，系统优先改善记录，不调整；
* [ ] 当天只记一顿饭计为当日覆盖，存在漏餐信号时标记疑似漏记。

### 饮食记录

* [ ] 用户可以通过自然语言记录饮食；
* [ ] 用户可以通过图片辅助记录饮食；
* [ ] 图片识别结果能够返回食物及营养估算；
* [ ] 热量估算使用单一近似值；
* [ ] 不确定信息能够明确展示主要误差来源；
* [ ] 信息不足时 Agent 可以进行必要的关键追问；
* [ ] 用户确认后餐食才写入正式记录；
* [ ] 用户未确认时不会静默修改饮食记录；
* [ ] 餐食记录成功后今日已摄入和剩余预算能够更新；
* [ ] 待确认估算不计入饮食记录完整度；
* [ ] 系统能识别明显漏餐并生成追问或改善记录任务。

### 下一餐推荐

* [ ] Agent 可以结合用户当前 Context 推荐下一餐；
* [ ] 推荐能够考虑今日剩余热量；
* [ ] 推荐能够考虑当前餐次预算；
* [ ] 推荐能够考虑用户饮食偏好和忌口；
* [ ] DIY 路径可以根据现有食材和烹饪条件生成可执行食谱；
* [ ] DIY 推荐能够提供食材、克数、步骤、耗时和近似热量；
* [ ] 到店路径可以调用真实餐厅数据进行推荐；
* [ ] 餐厅推荐不会违反用户明确的过敏或忌口要求；
* [ ] 推荐结果优先给出明确主推荐，而不是一次返回大量候选让用户重新筛选。

### Agent 能力

* [ ] Agent 可以读取完成当前任务需要的用户 Context；
* [ ] Agent 可以读取与当前任务相关的长期 Memory；
* [ ] Agent 不会要求用户重复提供系统中已经存在的关键信息；
* [ ] Agent 可以根据当前任务选择合适的 Skill / Tool；
* [ ] Agent 可以根据 Tool 返回结果判断是否继续执行；
* [ ] Tool 结果不足时 Agent 可以调整执行计划；
* [ ] Agent 设置最大执行轮次，避免无限 Tool 调用；
* [ ] 涉及用户数据修改时采用 Human-in-the-loop 确认流程。

### 外部服务与降级

* [ ] 外部 Tool 调用失败时系统不会编造成功结果；
* [ ] 餐厅数据无法获取时能够进入降级路径；
* [ ] 菜单无法核验时不会生成虚假的实时菜单；
* [ ] 营养数据无法精确获取时能够明确标记为估算；
* [ ] 降级结果能够向用户说明数据限制与可信度；
* [ ] 外部服务失败不会导致 Agent 无限重试。

---

## 12.2 Agent Demo 成功标准

Demo 应能够清楚展示：

```text
长期目标
+
结构化状态
+
Context
+
Memory
+
Planning
+
Skill
+
Tool Use
+
Observation
+
必要时 Re-plan
+
Multimodal
+
Human-in-the-loop
+
反馈闭环
```
---

# 13. 一句话项目介绍

> **小饭是一款面向减脂用户的个人饮食决策 Agent。它持续理解用户的减脂目标、体重趋势和当天饮食状态，通过长期记忆、工具调用、多模态理解与动态规划，把复杂的饮食约束转化为用户可以直接执行的下一餐决策。**
